[project]
name = "lab1"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Nanashamas", email = "104678426+Nanashamas@users.noreply.github.com" }
]
requires-python = ">=3.10"
dependencies = []

[project.scripts]
lab1 = "lab1:main"

[build-system]
requires = ["uv_build>=0.12.15,<0.13.0"]
build-backend = "uv_build"
